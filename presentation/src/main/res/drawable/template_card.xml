<?xml version="1.0" encoding="utf-8"?>
<!-- Rounded card used for each template row: a bubble-coloured surface with a ripple clipped
     to the same rounded shape. bubbleColor and colorControlHighlight both follow the theme. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:attr/colorControlHighlight">

    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="12dp" />
        </shape>
    </item>

    <item>
        <shape android:shape="rectangle">
            <solid android:color="?attr/bubbleColor" />
            <corners android:radius="12dp" />
        </shape>
    </item>

</ripple>
